<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zhinanzhen.b.dao.PortalLogDAO">

    <cache eviction="LRU" flushInterval="120000" readOnly="false" size="100"></cache>

    <insert id="addPortalLog" parameterType="org.zhinanzhen.b.dao.pojo.PortalLogDO" useGeneratedKeys="true" keyProperty="id">
        INSERT
            INTO b_portal_log (
                portal_id,
                gmt_create,
                operator_id,
                operator_name,
                role,
                action,
                from_state,
                to_state,
                content,
                ip,
                user_agent)
            VALUES (
                #{portalId},
                now(),
                #{operatorId},
                #{operatorName},
                #{role},
                #{action},
                #{fromState},
                #{toState},
                #{content},
                #{ip},
                #{userAgent});
    </insert>

    <select id="listPortalLog" resultType="org.zhinanzhen.b.dao.pojo.PortalLogDO">
        SELECT
            id,
            portal_id AS portalId,
            gmt_create AS gmtCreate,
            operator_id AS operatorId,
            operator_name AS operatorName,
            role,
            action,
            from_state AS fromState,
            to_state AS toState,
            content,
            ip,
            user_agent AS userAgent
        FROM b_portal_log
        WHERE 1 = 1
        <if test='portalId != null'>
            AND portal_id = #{portalId}
        </if>
        ORDER BY id DESC
        LIMIT #{offset}, #{rows};
    </select>

    <select id="countPortalLog" resultType="java.lang.Integer">
        SELECT count(*)
        FROM b_portal_log
        WHERE 1 = 1
        <if test='portalId != null'>
            AND portal_id = #{portalId}
        </if>
        ;
    </select>

</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zhinanzhen.b.dao.FileMaraAnnotationDAO">

    <cache eviction="LRU" flushInterval="120000" readOnly="false" size="100"></cache>

    <resultMap id="fileMaraAnnotationResultMap" type="org.zhinanzhen.b.dao.pojo.FileMaraAnnotationDO">
        <id property="id" column="id"/>
        <result property="gmtCreate" column="gmt_create"/>
        <result property="gmtModify" column="gmt_modify"/>
        <result property="serviceOrderId" column="service_order_id"/>
        <result property="userId" column="user_id"/>
        <result property="officialId" column="official_id"/>
        <result property="cloudDiskFileId" column="cloud_disk_file_id"/>
        <result property="isAnnotation" column="is_annotation"/>
        <result property="isCheck" column="is_check"/>
    </resultMap>

    <insert id="add" parameterType="org.zhinanzhen.b.dao.pojo.FileMaraAnnotationDO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO b_file_mara_annotation (
            gmt_create,
            gmt_modify,
            service_order_id,
            user_id,
            official_id,
            cloud_disk_file_id,
            is_annotation,
            is_check
        ) VALUES (
            now(),
            now(),
            #{serviceOrderId},
            #{userId},
            #{officialId},
            #{cloudDiskFileId},
            #{isAnnotation},
            #{isCheck}
        );
    </insert>

    <update id="update" parameterType="org.zhinanzhen.b.dao.pojo.FileMaraAnnotationDO">
        UPDATE b_file_mara_annotation
        SET gmt_modify = now()
        <if test="serviceOrderId > 0">
            , service_order_id = #{serviceOrderId}
        </if>
        <if test="userId > 0">
            , user_id = #{userId}
        </if>
        <if test="officialId > 0">
            , official_id = #{officialId}
        </if>
        <if test="cloudDiskFileId > 0">
            , cloud_disk_file_id = #{cloudDiskFileId}
        </if>
        <if test="isAnnotation != null">
            , is_annotation = #{isAnnotation}
        </if>
        <if test="isCheck != null">
            , is_check = #{isCheck}
        </if>
        WHERE id = #{id};
    </update>

    <select id="getById" resultMap="fileMaraAnnotationResultMap">
        SELECT
            id,
            gmt_create,
            gmt_modify,
            service_order_id,
            user_id,
            official_id,
            cloud_disk_file_id,
            is_annotation,
            is_check
        FROM b_file_mara_annotation
        WHERE id = #{id};
    </select>

    <select id="list" resultMap="fileMaraAnnotationResultMap">
        SELECT
            id,
            gmt_create,
            gmt_modify,
            service_order_id,
            user_id,
            official_id,
            cloud_disk_file_id,
            is_annotation,
            is_check
        FROM b_file_mara_annotation
        WHERE 1 = 1
        <if test="serviceOrderId != null">
            AND service_order_id = #{serviceOrderId}
        </if>
        <if test="userId != null">
            AND user_id = #{userId}
        </if>
        <if test="officialId != null">
            AND official_id = #{officialId}
        </if>
        ORDER BY id DESC;
    </select>

    <delete id="deleteById" parameterType="java.lang.Integer">
        DELETE FROM b_file_mara_annotation WHERE id = #{id, jdbcType=INTEGER};
    </delete>

</mapper>
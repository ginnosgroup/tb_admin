<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zhinanzhen.b.dao.WechatUserTagDAO">

	<insert id="addWechatUserTag" parameterType="org.zhinanzhen.b.dao.pojo.WechatUserTagDO" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
		insert into b_wechat_user_tag(
		gmt_create,
		gmt_modify,
		user_id,
		tag_id,
		tag_name,
		group_name,
		type)
		values (
		now(),
		now(),
		#{userId},
		#{tagId},
		#{tagName},
		#{groupName},
		#{type})
	</insert>

	<delete id="deleteWechatUserTagByUserId">
		delete from b_wechat_user_tag where user_id = #{userId}
	</delete>

	<select id="listWechatUserTagByUserId" resultType="org.zhinanzhen.b.dao.pojo.WechatUserTagDO">
		SELECT id, gmt_create AS gmtCreate, user_id AS userId, tag_id AS tagId,
			tag_name AS tagName, group_name AS groupName, type
		FROM b_wechat_user_tag
		WHERE user_id = #{userId}
	</select>

</mapper>

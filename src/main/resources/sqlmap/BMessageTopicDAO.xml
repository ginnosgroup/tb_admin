<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zhinanzhen.b.dao.MessageTopicDAO">

    <insert id="addMessageTopic" parameterType="org.zhinanzhen.b.dao.pojo.MessageTopicDO" useGeneratedKeys="true" keyProperty="id">
        INSERT
            INTO b_message_topic (
                gmt_create,
                gmt_modify,
                user_id,
                title,
                content)
            VALUES (
                now(),
                now(),
                #{userId},
                #{title},
                #{content});
    </insert>
    
    <select id="listMessageTopic" resultType="org.zhinanzhen.b.dao.pojo.MessageTopicDO">
        SELECT
            id,
            gmt_create AS gmtCreate,
            user_id AS userId,
            title,
            content
        FROM b_message_topic
        WHERE 1 = 1
        <if test='userId != null'>
            AND user_id = #{userId}
        </if>
        ORDER BY id ASC;
    </select>
    
    <delete id="deleteMessageTopic" parameterType="java.lang.Integer">
		DELETE FROM b_message_topic WHERE id = #{id, jdbcType=INTEGER}
	</delete>

</mapper>
